## FD-ORGANIZER (File-Directory Organizer)
A simple CLI utility that organizes the files of a directory based on a mapping.

### MAPPING (Quick Guide)
A mapping is simply a 'JSON' file with objects that only have 